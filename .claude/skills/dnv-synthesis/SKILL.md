---
name: dnv-synthesis
description: Синтезирует единый источник правды user/spec.md из профиля кейса + курируемой базы (knowledge_base/) + живых находок (user/research-notes.md) + обезличенного дайджеста практики. Каждый факт с тегом достоверности и as-of; заполняет плейсхолдеры шаблона из профиля. Требует dnv-research.
---

# dnv-synthesis — единый источник правды (user/spec.md)

Собираешь `user/spec.md` — обезличенный (значения из профиля, не дублируются
наружу) единый источник правды с **тегами достоверности и as-of** по шаблону
`templates/spec_renewal.template.md`.

## Предусловие (см. `user/pipeline-state.schema.md`)

- Твёрдое: `dnv-research` = `completed` (есть `user/research-notes.md`) и есть
  `user/case-profile.json`. Нет — **остановись**: «Сначала `/dnv-research`».
- Мягкое: `dnv-chat-mining`. Если **не** `completed` — работай, но **пометь**,
  что практика взята только из опубликованного дайджеста
  (`knowledge_base/practice/`), без свежих локальных slices (полнота снижена).

## Шаг 1. Собери входы

1. `user/case-profile.json` — значения для `{{ПЛЕЙСХОЛДЕРОВ}}`.
2. `user/research-notes.md` — **живые находки** (с источниками и as-of).
3. `knowledge_base/sources/primary/*` — **дословные тексты** законов и Instrucción
   (BOE + inclusion.gob.es, с датой извлечения). **Единственный якорь тега
   `[норма]`.** Цитируя норму в spec, бери формулировку отсюда.
4. `knowledge_base/norms/*` — разбор норм + **as-of** даты. Это *чтение* первичного
   текста: шапка каждого файла называет файл и статьи в `sources/primary/`, на
   которые он якорится. **При расхождении `norms/` ↔ `primary/` прав `primary/`** —
   зафиксируй расхождение в §11.
5. `knowledge_base/practice/digest.md` — практика (мнение). ⚠️ Пункт дайджеста **не
   знает, к какому кейсу применим**: прежде чем поднимать из него блокер, сверься с
   нормативной рамкой (напр. «а как он въедет» — см. `norms/solicitud-inicial.md` §3,
   где три ситуации разграничены и для двух проблемы нет).
6. `knowledge_base/norms/solicitud-inicial.md` — **при `case.process_type == initial`
   читать обязательно**: маршрут (74 quáter visado vs 74 quinquies изнутри),
   законность пребывания и declaración de entrada, кто подписывает без NIE,
   обязательство по Seguridad Social и риск extinción.
7. `templates/spec_renewal.template.md` — структура spec (0–12).
   **Раздел «2-bis. Маршрут первичной подачи» заполняется только при `initial`**;
   при `renovación` он удаляется целиком, а заполняется «Окно подачи». И наоборот —
   при `initial` удаляется «Окно подачи», потому что у первичной подачи окна нет.

## Шаг 2. Слей в spec

Заполни каждую секцию шаблона:

- **Плейсхолдеры `{{...}}`** — подставь из профиля. Нет значения → оставь
  `[ТРЕБУЕТСЯ: …]`, **не выдумывай** (особенно REGAGE/expediente — их нет до
  подачи; §0 остаётся «не подано»).
- **Каждое утверждение — с тегом** `[норма] | [официальное разъяснение] |
  [практика — консультант] | [практика — Telegram] | [не подтверждено]`.
- **Пороги/процедурное** — бери **живые** значения из `research-notes` (с годом +
  источником); если research-notes дал `[не подтверждено]` —
  сохрани пометку «(проверить вживую)», не подставляй устаревшее из базы как факт.
- **Приоритет источников при конфликте:** живой `[норма]` (BOE) >
  **`sources/primary/` (дословный текст)** > `norms/` (разбор) >
  `[официальное разъяснение]` > `[практика]`. Расхождения база↔живое фиксируй в
  §11 «Расхождения».
- **Тег `[норма]` проверяем:** утверждение, которое не подтверждается дословным
  текстом в `sources/primary/` (или живым BOE), тег `[норма]` нести **не может** —
  максимум `[официальное разъяснение]`. Не повышай уровень «потому что звучит как
  закон».
- **Практика** (§4.7, §2 уточнения) — из дайджеста, с явным «Telegram = мнение».

## Шаг 3. Шапка достоверности прохода

В начало `user/spec.md` добавь блок:

```markdown
> **Синтез от:** <дата>  ·  **Рисёрч от:** <дата research-notes>
> **Практика:** <дайджест | дайджест + свежие slices>
> As-of норм-базы: 2026-06 (перепроверено вживую там, где помечено).
```

## Шаг 4. Приватность

Значения профиля идут **только** в `user/spec.md` (gitignored). **Не** дублируй
NIE/имена/суммы/адрес в `CLAUDE.md`, память или любой коммитимый файл.

## Шаг 5. Обнови состояние

`steps["dnv-synthesis"] = {status:"completed", last_run:"<сегодня>",
output:"user/spec.md"}`.

## Выход

`user/spec.md` — все секции 0–12, теги на каждом факте, as-of в шапке,
плейсхолдеры подставлены или `[ТРЕБУЕТСЯ]`. Следующий шаг: `dnv-verify`
(devils-advocate сверит claim'ы). Затем `dnv-documents` (если ещё не сделаны).

> `user/spec.md` — **черновик-ориентир**, не юридическая консультация и не подача
>. Каждый `[не подтверждено]` — в «список внешних проверок» верификации.
