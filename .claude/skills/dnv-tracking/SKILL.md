---
name: dnv-tracking
description: Внутренний справочник по пост-подачному ведению кейса DNV. Расчёт дат (срок рассмотрения, silencio positivo, дедлайн requerimiento) из даты подачи по knowledge_base/norms/plazos-silencio.md, ведение user/tracking.md, обработка requerimiento, лестница эскалации, escrito-шаблоны, мониторинг DEHú. Движок ничего не подаёт.
---

# Ведение кейса после подачи — внутренний справочник

Когда пользователь сообщил, что подал заявление, ты ведёшь `user/tracking.md` —
живой журнал кейса. Логика каждого обращения: что изменилось → пересчитать даты →
сказать, что делать сейчас.

## 🚫 Необратимые действия — не твои

Движок **не подаёт, не платит tasa, не регистрирует expediente и не выдумывает
номера**. Факт подачи, дату и REGAGE сообщает **пользователь** — со своего
justificante. Нет данных → `[ТРЕБУЕТСЯ: …]`, никогда не правдоподобная догадка.

## Что изменилось?

- Если `user/tracking.md` **нет** — это первое обращение: спроси, подано ли уже
  заявление; если да — **дату подачи** и **REGAGE** (как в justificante).
  Если ещё не подано — скажи, что ведение начнётся после подачи.
- Если файл **есть** — прочитай его и спроси, что нового с даты последнего
  обновления: пришло ли что-то в **DEHú** (requerimiento? resolución?),
  менялся ли estado в Carpeta Ciudadana, отвечал ли пользователь на что-то.

## Пересчёт дат

Из **даты подачи** (от пользователя) и `knowledge_base/norms/plazos-silencio.md`
посчитай конкретные календарные даты:

- конец **срока рассмотрения** (plazo de resolución);
- точка, с которой можно ссылаться на **silencio positivo** (art. 76 Ley
  14/2013) — с оговоркой «механику и точку отсчёта сверить вживую»;
- если пользователь сообщил о **requerimiento** из DEHú — **дедлайн ответа**
  (срок тикает с даты уведомления в DEHú, не с даты, когда его прочитали руки);
- для renovación — не истёк ли TIE и что это меняет.

**Даты — арифметика из сообщённых пользователем дат + нормы, не выдумка.**
Норм-файл несёт as-of: если он старше текущего года, пометь «(проверить
вживую)» рядом с рассчитанной датой.

## Структура `user/tracking.md`

Пиши/перезаписывай файл (gitignored; значения — только здесь):

```markdown
# Ведение кейса — обновлено <дата>

## Ключевые факты (от пользователя)
- Подано: <дата> · REGAGE: <номер | [ТРЕБУЕТСЯ]>
- Последнее событие: <что, когда, канал>

## Рассчитанные даты
| Дата | Что наступает | Основание (норм-файл, as-of) | Статус |

## Что делать сейчас
1. <конкретное действие с датой>

## Когда какой escrito становится актуален
| Ситуация | С какой даты | Шаблон |
```

Секцию escritos заполняй из плейбука эскалации (см. справочник по подготовке
пакета, раздел «Плейбук эскалации»): recordatorio → silencio positivo →
agilización → queja Defensor → recurso (последний — к юристу). Ссылайся на
`templates/escrito_*.template.md`; заполненные escritos пиши в `user/drafts/`.

## Доска задач (если подключена)

Если пользователь завёл доску (Trello / Notion / GitHub Issues) — предложи
синхронизировать: закрыть сделанные карточки, добавить новые дедлайны из «Что
делать сейчас». **PII-правило то же:** только generic-названия задач и даты;
никаких profile-derived значений (ФИО, NIE/паспорт, REGAGE/expediente, адреса,
телефоны/email, работодатели/клиенты/банки, суммы).

## Переход к TIE (когда пришло решение)

Как только пользователь сообщил, что пришла **resolución favorable** или
**certificado de acto presunto**, — ведение сроков здесь заканчивается. Зафиксируй
факт и дату **уведомления** в `user/tracking.md` и скажи, что дальше — последняя
миля: TIE, huellas, EX-17, tasa 790-012, recogida (см. справочник по TIE).

Escritos, huellas, tasa 790-012 и recogida **здесь не разворачивай** — это зона
справочника по TIE, и два места правды по срокам последней мили не нужны. Отказ
(denegación) — наоборот, остаётся здесь: это ветка эскалации.

## Выход

Обновлённый `user/tracking.md` + короткий ответ в чат: ближайший дедлайн и
одно-два действия. Напомни каналы мониторинга (DEHú, Carpeta Ciudadana) и что
расчёты — черновик, требующий сверки с нормой вживую; движок не подаёт и не
отвечает на requerimiento за пользователя.
