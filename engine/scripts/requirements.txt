# spain-dnv-consultant engine scripts — Python dependencies
#
# The engine scripts use the Python STANDARD LIBRARY ONLY (json, re, argparse,
# pathlib, hashlib, hmac). There are no third-party pip dependencies to install.
#
# Requirements:
#   * Python 3.8 or newer (any OS: Windows / macOS / Linux).
#
# Optional, NOT installed via pip (separate, opt-in):
#   * Trello / Notion MCP servers (optional task board)  -> see dnv-submission
#   * pandoc + lualatex                -> only for the optional PDF build step
#
# (Intentionally empty of pip packages.)
